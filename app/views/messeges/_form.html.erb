<%= form_for(@messege) do |f| %>
  <% if @messege.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@messege.errors.count, "error") %> prohibited this messege from being saved:</h2>

      <ul>
      <% @messege.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :topic %><br />
    <%= f.text_field :topic %>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
	<% unless @messege.new_record? %>
	    <%= link_to 'publish', messege_change_state_path(@messege, state: :publish), method: :put %> |
	    <%= link_to 'unpublish', messege_change_state_path(@messege, state: :unpublish), method: :put %> |
	    <%= link_to 'delete', messege_change_state_path(@messege, state: :delete), method: :put %>
	  <% end %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
